# Constructor Inputs

Constructor inputs are values passed when a contract is deployed.

Examples:
- Token name
- Token symbol
- Initial supply
- Owner address
- Receiver address

Before deploying, double-check every constructor input. After deployment, constructor inputs usually cannot be changed.
