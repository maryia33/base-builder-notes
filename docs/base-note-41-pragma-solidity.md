# Pragma Solidity

Solidity files specify the compiler version with pragma.

Example:

```solidity
pragma solidity ^0.8.20;
```

This tells the compiler what version range is acceptable. Using the wrong compiler version can cause errors or unexpected behavior.
